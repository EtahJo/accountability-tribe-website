export type HeaderButton = {
  name: string;
};
export type SquareComponentType = {
  header: string;
  description: string;
};
export type CustomInputType = {
  name: string;
  type?: string;
  placeholder: string;
  textArea?: boolean;
  required?: boolean;
};
